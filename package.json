{
  "name": "phex-autoseed",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test && bash -n scripts/install.sh"
  },
  "engines": {
    "node": ">=24"
  }
}
